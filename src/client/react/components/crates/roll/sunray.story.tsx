import { hoarcekat } from "@rbxts/pretty-react-hooks";
import { Sunray } from "./sunray";
import React from "@rbxts/react";

export = hoarcekat(() => (
	<Sunray Position={new UDim2(0.5, 0, 0.5, 0)} Size={new UDim2(0.2, 0, 0.2, 0)} looped={true} />
));
